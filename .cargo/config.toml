# Developer environment configuration for Isolate.
#
# Warnings are treated as errors to match CI behavior.
# To temporarily disable, comment out the [build] section below.
#
# Task runner shortcuts:
#   cargo xtask check
#   just check

[build]
rustflags = ["-Dwarnings"]

[alias]
xtask = "run --package xtask --"
